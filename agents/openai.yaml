interface:
  display_name: "🦁 XXD Panel 054"
  short_description: "Build selective-memory art with six unequal stickers"
  default_prompt: "Use $xxd-panel-054 with my photograph, ask for modes and copy settings, then create fresh raster outputs."
policy:
  allow_implicit_invocation: true
