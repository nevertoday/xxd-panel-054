interface:
  display_name: "/xxd-panel-054"
  short_description: "Create Panel 054 from its original style brief."
  default_prompt: "Use $xxd-panel-054; keep its bundled original style brief as the sole aesthetic authority, adapt questions to real host controls, ask only for unresolved settings, and never reject a paired result solely because its generated boundary is slightly off the mathematical midpoint."

policy:
  allow_implicit_invocation: true
