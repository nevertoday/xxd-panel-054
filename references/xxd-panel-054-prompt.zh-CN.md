# XXD Panel 054｜运行适配器（中文）

本文件不是第二套美学提示词。生成时必须先完整读取并逐字使用 `references/054-source.md`；它是 054 的唯一创作与审美权威。本文件只负责把用户本次选择的运行变量追加到原始提示词末尾。

## 不可改写的边界

- 不总结、翻译、扩写或“优化”原始提示词。
- 不额外规划颜色、色板、材料、构图、留白、标题、微文案或美学动机。
- 原始提示词中的颜色处理逻辑保持原样：它要求从照片取色就取色，它规定固定色系就使用固定色系。
- 原始提示词中的文字逻辑由图像模型执行；外层 Skill 不提前写标题或文案包。
- 用户选择的模式和尺寸只覆盖旧的 3:4 上下双联容器，不覆盖其余审美规则。

## 末尾追加模板

```text
【本次运行覆盖｜只改变交付容器】

原始提示词最初的 3:4 上下结构，是旧版展示容器。
本次只覆盖：最终画幅、原图与设计区域的位置、原图是否显示、设备尺寸。
除上述容器变量外，原始提示词中的设计转译、构图、颜色、材料、留白、
文字气质与排版逻辑全部保持有效。

“上半部分照片”映射为 SOURCE REFERENCE。
“下半部分设计”映射为 DESIGN REGION。

输出模式：TOP_BOTTOM｜LEFT_RIGHT｜DESIGN_ONLY｜WALLPAPER_PACK
最终画布：<比例和／或准确 WIDTHxHEIGHT>
设备：NONE｜PHONE｜IPAD｜DESKTOP｜WATCH
原图可见性：上半区｜左半区｜仅作参考且不显示
设计区域：下半区｜右半区｜完整画布
双联关系：服从原始提示词自己的配对比例规则｜不适用
壁纸关系：NONE｜INDEPENDENT｜LINKED

颜色完全服从原始提示词已有的颜色处理逻辑。
除非用户明确提出颜色修改，否则不要增加、替换、概括或重新规划任何色盘。
```

在运行覆盖块后只追加一种文字模式。若用户还有其他明确要求，将其逐字放在文字模式块之后，也就是全部内容最末尾。

### 模型根据原始提示词生成文字

```text
TEXT MODE: ORIGINAL_PROMPT_GENERATED
TEXT LANGUAGE: <用户确认的语言或地区>

由图像模型按照原始提示词已有的文字生成逻辑生成。
外层 Skill 不预先编写标题、微文案、文案包或额外语义框架。
```

### 使用用户准确文字

```text
TEXT MODE: USER_EXACT
TEXT LANGUAGE: <用户确认的语言或地区>
TEXT: 「<用户准确文字>」

逐字使用，不改写、不翻译、不纠错、不添加其他文案。
排版方式与字体气质继续服从原始提示词。
```

### 不要文字

```text
TEXT MODE: NONE
画面中不得出现任何字母、文字、数字、标题、标签、Logo 或伪文字。
```

执行、问询、尺寸、多模式、壁纸与输出目录规则以 `SKILL.md` 为准。无论何种模式，最终生成请求始终是：

```text
054-source.md 原文（完整、逐字）
+ 本次运行覆盖块
+ 一种文字模式块
+ 用户明确提出的其他准确要求（若有，逐字放在最后）
```
